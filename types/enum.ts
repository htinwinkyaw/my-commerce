export enum ProductFormMode {
  Add,
  Edit,
}

export enum AddressItemMode {
  Edit, // for editing the address
  View, // for dispalying the addresses in profile
  Change, // for changing the address as default
}

export enum AddressFormMode {
  Edit,
  Create,
}
